# Normalize line endings so the released tree is identical on every platform.
* text=auto eol=lf

# Windows batch helpers must keep CRLF.
*.bat text eol=crlf

# Binary artifacts are never touched.
*.png binary
*.jpg binary
*.ico binary
*.lib binary
*.exe binary
*.dll binary
